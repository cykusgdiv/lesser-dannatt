reduce search match chars, e.g. initial 3, adjust to 2 (try to search "sýndisk", irrelevant "sýndr" appears because it matches with first 4 characters, more than correct "sýna" which matches with only the first 3)

allow user to specify part-of-speech